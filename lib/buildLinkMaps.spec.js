const buildLinkMaps = require("./buildLinkMaps.js");
const { countFiles } = require("../test/fileUtils.js");
const Logger = require("./utils/logger.js");
const log = new Logger("info");

describe("buildLinkMaps()", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
